{% macro plot(pt) -%}
<h2>{{ pt.title.html | safe }}</h2>
<div>
    <!-- <figure class="figure">
        <img src="{{ pt.path }}" class="figure-img img-fluid rounded" alt="{{ pt.title }}">
        {% if pt.caption %}
        <figcaption class="figure-caption">{{ pt.caption.html | safe }}</figcaption>
        {% endif %}
    </figure> -->
    {% if pt.embeddable %}
    {{ pt.div | safe}}
    {% endif %}
    <p>{{ pt.description.html | safe }}</p>
</div>
{%- endmacro %}
